/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "/portfolio",
    output: "export",  // <=== pour git page, ne pas laisser
    reactStrictMode: true,
};

export default nextConfig;
